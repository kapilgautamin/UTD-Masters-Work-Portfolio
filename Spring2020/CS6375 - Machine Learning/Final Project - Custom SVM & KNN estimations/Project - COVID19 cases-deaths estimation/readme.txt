If using Jupyter:
	1. Install required dependencies by running the commands below:
		a. pip install numpy
		b. pip install pandas
		c. pip install scikit-learn
		d. pip install xgboost
		e. pip install matplotlib
		f. pip install cvxopt
	2. Run the code.ipynb file using Jupyter

If using Google Colab:
	1. Run the code.ipynb file in Google Colab